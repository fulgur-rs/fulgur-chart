{
  "name": "@fulgur-rs/chart-wasm",
  "version": "0.1.0",
  "description": "Deterministic chart.js v4 / Vega-Lite JSON to SVG/PNG renderer (WebAssembly)",
  "type": "module",
  "main": "index.js",
  "module": "index.js",
  "types": "index.d.ts",
  "license": "MIT OR Apache-2.0",
  "engines": {
    "node": ">=18.17"
  },
  "files": [
    "index.js",
    "index.d.ts",
    "pkg/"
  ],
  "scripts": {
    "build": "wasm-pack build --target web --release --locked",
    "build:no-default-font": "wasm-pack build --target web --release --locked --no-default-features --features no-default-font",
    "test": "node --test",
    "test:no-default-font": "npm run build:no-default-font && node __test__/no-default-font-smoke.mjs && cargo test --manifest-path ../../fulgur-chart/Cargo.toml --no-default-features --locked --lib",
    "test:browser": "node --test __test__/browser-smoke.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "prepack": "npm run build && rm -f pkg/.gitignore"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "playwright": "^1.61.1",
    "typescript": "^5.0.0"
  }
}
